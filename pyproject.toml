[project]
name = "time-series"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "numpy>=2.3.1",
    "scikit-learn>=1.7.0",
    "torch>=2.7.1",
]
